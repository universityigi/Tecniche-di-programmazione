#include <stdio.h>
#include <math.h>

int main()
{
    float x, R0, RN1, RN;
    printf("Inserisci il numero di cui fare la radice quadrata: ");
    scanf("%d",&N);
    RN = (RN1 + x / RN1) / 2;
    while(RN<x)
    {
        double floor(double x)
        RN = (RN1 + x / RN1) / 2;
    }

}
